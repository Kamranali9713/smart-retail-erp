"use client";import{useEffect,useState}from"react";
export default function AccountsPage(){const[rows,setRows]=useState([]);const[form,setForm]=useState({name:"",type:"ASSET"});const load=()=>fetch('/api/accounting/accounts').then(r=>r.json()).then(setRows);useEffect(load,[]);async function save(e){e.preventDefault();await fetch('/api/accounting/accounts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setForm({name:"",type:"ASSET"});load();}return <div className="space-y-4"><div><h1 className="text-2xl font-bold">Chart of Accounts</h1><p className="text-sm text-muted-foreground">Manage the accounts used by the accounting ledger.</p></div><form onSubmit={save} className="flex gap-2 bg-card border border-border rounded-lg p-4"><input required placeholder="Account name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="border border-border rounded-md px-3 py-2 bg-background flex-1"/><select value={form.type} onChange={e=>setForm({...form,type:e.target.value})} className="border border-border rounded-md px-3 py-2 bg-background"><option>ASSET</option><option>LIABILITY</option><option>EQUITY</option><option>REVENUE</option><option>EXPENSE</option></select><button className="bg-primary text-primary-foreground rounded-md px-4">Add Account</button></form><div className="bg-card border border-border rounded-lg overflow-x-auto"><table className="w-full text-sm"><thead className="bg-secondary"><tr><th className="p-3 text-left">Account</th><th className="p-3 text-left">Type</th><th className="p-3 text-right">Balance</th></tr></thead><tbody>{rows.map(a=><tr key={a.id} className="border-t border-border"><td className="p-3">{a.name}</td><td className="p-3">{a.type}</td><td className="p-3 text-right">Rs. {Number(a.balance).toFixed(2)}</td></tr>)}</tbody></table></div></div>}
