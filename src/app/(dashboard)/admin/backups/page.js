"use client";
import { useEffect, useState } from "react";
export default function BackupsPage(){const [items,setItems]=useState([]);const load=()=>fetch('/api/admin/backups').then(r=>r.json()).then(setItems);useEffect(load,[]);async function request(){await fetch('/api/admin/backups',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({})});load();}return <div className="space-y-4"><div className="flex justify-between items-center"><div><h1 className="text-2xl font-bold">Backups</h1><p className="text-sm text-muted-foreground">Track database backup requests. Actual pg_dump execution can be attached to your server deployment.</p></div><button onClick={request} className="px-4 py-2 rounded-md bg-primary text-primary-foreground">Request Backup</button></div><div className="bg-card border border-border rounded-lg overflow-x-auto"><table className="w-full text-sm"><thead className="bg-secondary"><tr><th className="p-3 text-left">Created</th><th className="p-3 text-left">File</th><th className="p-3 text-left">Size</th><th className="p-3 text-left">Status</th></tr></thead><tbody>{items.map(x=><tr key={x.id} className="border-t border-border"><td className="p-3">{new Date(x.createdAt).toLocaleString()}</td><td className="p-3 font-mono text-xs">{x.fileName}</td><td className="p-3">{Number(x.sizeBytes||0).toLocaleString()} bytes</td><td className="p-3">{x.status}</td></tr>)}</tbody></table></div></div>}
