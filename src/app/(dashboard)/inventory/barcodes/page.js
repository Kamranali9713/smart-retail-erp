"use client";
import { useEffect, useRef, useState } from "react";
import JsBarcode from "jsbarcode";
function Label({product}){const ref=useRef(null);useEffect(()=>{if(ref.current) JsBarcode(ref.current,product.barcode||product.sku,{format:"CODE128",displayValue:true,height:45,margin:6});},[product]);return <div className="border border-border rounded-lg p-3 bg-white text-black text-center print:break-inside-avoid"><div className="font-semibold text-sm">{product.name}</div><svg ref={ref} className="mx-auto"/><div className="text-sm">Rs. {Number(product.sellingPrice).toFixed(2)}</div></div>}
export default function BarcodeLabelsPage(){const [products,setProducts]=useState([]);const [q,setQ]=useState("");useEffect(()=>{fetch(`/api/products?q=${encodeURIComponent(q)}`).then(r=>r.json()).then(setProducts);},[q]);return <div className="space-y-4"><div className="flex justify-between items-center"><div><h1 className="text-2xl font-bold">Barcode Labels</h1><p className="text-sm text-muted-foreground">Generate printable product barcode labels for testing and store use.</p></div><button onClick={()=>window.print()} className="px-4 py-2 rounded-md bg-primary text-primary-foreground">Print Labels</button></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products" className="border border-border rounded-md px-3 py-2 bg-background w-full max-w-md"/><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">{products.map(p=><Label key={p.id} product={p}/>)}</div></div>}
